sciencehub/  — Repo root; everything needed to build, run, test, and ship ScienceHub lives here.
├─ README.md  — What ScienceHub is, quick start, screenshots, and “how to use”.
├─ LICENSE  — Legal license text for reuse and distribution.
├─ CHANGELOG.md  — Human-readable release notes by version/date.
├─ CONTRIBUTING.md  — How others can contribute: rules, style, PR flow, checks.
├─ CODE_OF_CONDUCT.md  — Community behavior expectations and moderation policy.
├─ CITATION.cff  — Citation metadata so people can cite ScienceHub properly.
├─ pyproject.toml  — Python project config: build, packaging, tooling, dependencies.
├─ requirements.txt  — Simple dependency list (useful for quick installs/CI).
├─ package.json  — Frontend/JS tooling deps if you add web UI tooling later.
├─ .gitignore  — Files Git should not track (caches, builds, secrets, etc.).
├─ .editorconfig  — Editor formatting rules (indentation, newline, charset).
├─ .env.example  — Template for environment variables without secrets.
├─ Makefile  — Short commands for common tasks (test, lint, run, build).
├─ Dockerfile  — Container recipe for running ScienceHub reproducibly.
├─ docker-compose.yml  — Multi-service setup (app + db + worker) if needed.
├─ scripts/  — Developer automation scripts that aren’t part of the runtime app.
│  ├─ bootstrap.py  — Sets up dev environment (deps, folders, quick checks).
│  ├─ dev_run.py  — Starts the app in dev mode with helpful defaults.
│  ├─ build_release.py  — Builds distributables (wheel, installer assets, etc.).
│  ├─ clean_cache.py  — Clears caches/logs/temp files for a clean state.
│  ├─ migrate_data.py  — Runs data migrations for persistence formats.
│  └─ generate_docs.py  — Builds docs (API refs, markdown site, etc.).
│  └─sciencehub_init.py — Initializes package structure for new installs.
├─ sciencehub/  — Main Python package; all runtime code goes under here.
│  ├─ __init__.py  — Package marker; may expose version and public API.
│  ├─ app/  — Application lifecycle: startup, shutdown, context wiring.
│  │  ├─ __init__.py  — Marks app module and can expose app-level helpers.
│  │  ├─ entrypoint.py  — Primary entry; creates context and launches UI/CLI/API.
│  │  ├─ app_context.py  — Object holding shared services (kernel, engines, config).
│  │  ├─ app_state.py  — Global app state model (running mode, active session).
│  │  ├─ app_events.py  — Event definitions emitted during runtime (loaded, ran, failed).
│  │  ├─ app_registry.py  — Registry for services/tools/plugins so everything can find each other.
│  │  ├─ startup.py  — Startup routines (load config, init storage, warm caches).
│  │  ├─ shutdown.py  — Shutdown routines (flush logs, close DB, save state).
│  │  ├─ exception_handler.py  — Central error handling + user-friendly messages.
│  │  └─ versioning.py  — Version retrieval and compatibility checks.
│  ├─ kernel/  — Scientific “core”: symbols, units, expressions, solvers, validation.
│  │  ├─ __init__.py  — Kernel module exports and convenience imports.
│  │  ├─ symbols/  — Fundamental building blocks: units, dimensions, variables.
│  │  │  ├─ __init__.py  — Symbols package marker and exports.
│  │  │  ├─ symbol.py  — Defines a variable/parameter symbol and its metadata.
│  │  │  ├─ symbol_set.py  — Groups symbols for a problem (inputs/outputs/constants).
│  │  │  ├─ dimensions.py  — Dimensional system (L, T, M…) and dimension arithmetic.
│  │  │  └─ units.py  — Unit definitions + conversions + formatting rules.
│  │  ├─ expressions/  — Parsing and evaluating expressions and equations.
│  │  │  ├─ __init__.py  — Expressions package marker.
│  │  │  ├─ expression.py  — Core expression representation (AST or similar).
│  │  │  ├─ parser.py  — Turns user text/math into an expression structure.
│  │  │  ├─ evaluator.py  — Computes numeric results from expressions and symbol values.
│  │  │  ├─ simplifier.py  — Simplifies expressions (reduce, factor, canonicalize).
│  │  │  └─ latex_renderer.py  — Converts expressions into LaTeX for display/export.
│  │  ├─ solvers/  — Solvers for equations and systems (symbolic and numeric).
│  │  │  ├─ __init__.py  — Solvers package marker.
│  │  │  ├─ algebraic_solver.py  — Solves algebraic equations/systems.
│  │  │  ├─ numeric_solver.py  — Root finding and numeric system solving.
│  │  │  ├─ differential_solver.py  — ODE/PDE solving interfaces and integrators.
│  │  │  ├─ stochastic_solver.py  — Random-process solvers (SDEs, sampling).
│  │  │  └─ optimization_solver.py  — Minimization/maximization and constraint solving.
│  │  ├─ constants/  — Curated constants separated by domain for clarity and updates.
│  │  │  ├─ __init__.py  — Constants package marker.
│  │  │  ├─ physical_constants.py  — c, G, h, k_B, etc. with units and sources.
│  │  │  ├─ chemical_constants.py  — R, pKa references, standard potentials, etc.
│  │  │  ├─ astronomical_constants.py  — AU, solar mass, etc.
│  │  │  └─ biological_constants.py  — Common bio constants (if applicable) and defaults.
│  │  └─ validation/  — Guards against nonsense: unit mismatch, bounds, uncertainty.
│  │     ├─ __init__.py  — Validation package marker.
│  │     ├─ dimensional_analysis.py  — Checks unit/dimension consistency of equations.
│  │     ├─ bounds_checker.py  — Validates inputs (non-negative mass, domain limits, etc.).
│  │     └─ uncertainty_propagation.py  — Propagates measurement error through calculations.
│  ├─ domains/  — Domain-level science knowledge (formulas, models), not UI/IO.
│  │  ├─ __init__.py  — Domains package marker.
│  │  ├─ mathematics/  — Math utilities and models used across all sciences.
│  │  │  ├─ __init__.py  — Mathematics package marker.
│  │  │  ├─ core.py  — Shared math helpers (polynomials, matrices, transforms).
│  │  │  ├─ geometry.py  — Geometry models and helpers (vectors, shapes, angles).
│  │  │  ├─ calculus.py  — Derivatives, integrals, series, numeric approximations.
│  │  │  └─ statistics.py  — Distributions, descriptive stats, sampling utilities.
│  │  ├─ physics/  — Physics “model library” (equations and assumptions).
│  │  │  ├─ __init__.py  — Physics package marker.
│  │  │  ├─ mechanics.py  — Forces, energy, momentum, kinematics models.
│  │  │  ├─ thermodynamics.py  — Thermo relations, cycles, idealizations.
│  │  │  ├─ fluid_dynamics.py  — Flow, continuity, Bernoulli, viscosity basics.
│  │  │  ├─ statistical_mechanics.py  — Partition functions, ensembles (basic).
│  │  │  ├─ relativity.py  — SR basics: time dilation, Lorentz, energy-momentum.
│  │  │  ├─ quantum_basics.py  — Simple quantum relations and common models.
│  │  │  └─ nuclear_physics.py  — Decay, binding energy, simplified nuclear models.
│  │  ├─ chemistry/  — Chemistry models (reactions, equilibria, rates).
│  │  │  ├─ __init__.py  — Chemistry package marker.
│  │  │  ├─ stoichiometry.py  — Mole relations, limiting reagents, yields.
│  │  │  ├─ equilibrium.py  — K, Q, buffers, Le Chatelier logic helpers.
│  │  │  ├─ kinetics.py  — Rate laws, Arrhenius fits, reaction orders.
│  │  │  ├─ thermochemistry.py  — Enthalpy/entropy/Gibbs relations and estimates.
│  │  │  └─ electrochemistry.py  — Nernst, cell potentials, redox bookkeeping.
│  │  ├─ biology/  — Biology models (growth, genetics, enzyme kinetics).
│  │  │  ├─ __init__.py  — Biology package marker.
│  │  │  ├─ genetics.py  — Mendelian probabilities, Hardy–Weinberg, etc.
│  │  │  ├─ enzymology.py  — Michaelis–Menten style models and fits.
│  │  │  ├─ ecology.py  — Population growth models and stability.
│  │  │  └─ microbiology.py  — Simple microbial growth/decay and environment models.
│  │  ├─ astronomy/  — Space/astro models (orbits, stars, basic cosmology).
│  │  │  ├─ __init__.py  — Astronomy package marker.
│  │  │  ├─ orbits.py  — Kepler laws, transfer basics, orbital elements helpers.
│  │  │  ├─ stars.py  — Luminosity/temperature relations, magnitude tools.
│  │  │  └─ cosmology.py  — Redshift-distance basics and simple universe models.
│  │  ├─ geology/  — Earth science models and tables.
│  │  │  ├─ __init__.py  — Geology package marker.
│  │  │  ├─ minerals.py  — Mineral properties, indexing, and conversions.
│  │  │  └─ tectonics.py  — Plate motion helpers and simple estimators.
│  │  ├─ materials/  — Materials science models (stress/strain, crystals).
│  │  │  ├─ __init__.py  — Materials package marker.
│  │  │  ├─ crystal_structures.py  — Lattices, unit cells, packing factors.
│  │  │  └─ stress_strain.py  — Hooke’s law regions, moduli, deformation helpers.
│  │  ├─ electricity_magnetism/  — E&M models separated for clarity.
│  │  │  ├─ __init__.py  — E&M package marker.
│  │  │  ├─ circuits.py  — Ohm/Kirchhoff, RC/RL/RLC, power relations.
│  │  │  └─ fields.py  — Coulomb, Lorentz, Biot–Savart simplified cases.
│  │  ├─ waves_optics/  — Waves and optics models.
│  │  │  ├─ __init__.py  — Waves/Optics package marker.
│  │  │  ├─ waves.py  — Doppler, standing waves, dispersion basics.
│  │  │  └─ optics.py  — Lenses, diffraction, interference, polarization basics.
│  │  └─ interdisciplinary/  — Cross-domain helpers and estimation approaches.
│  │     ├─ __init__.py  — Interdisciplinary package marker.
│  │     ├─ unit_conversions.py  — High-level conversion helpers that use kernel.units.
│  │     └─ estimation.py  — Back-of-the-envelope and sanity-check utilities.
│  ├─ engines/  — Execution engines: calculation, simulation, animation, optimization.
│  │  ├─ __init__.py  — Engines package marker.
│  │  ├─ calculation/  — Deterministic “compute an answer” execution.
│  │  │  ├─ __init__.py  — Calculation package marker.
│  │  │  ├─ calculator_engine.py  — Main calculator runtime interface (inputs → results).
│  │  │  ├─ batch_calculator.py  — Runs many calculations (tables/sweeps).
│  │  │  └─ symbolic_calculator.py  — Symbolic manipulation and exact results path.
│  │  ├─ simulation/  — Time-based or stochastic simulations and stepping.
│  │  │  ├─ __init__.py  — Simulation package marker.
│  │  │  ├─ simulation_engine.py  — Core simulation loop abstraction.
│  │  │  ├─ time_evolution.py  — Integrators/steppers for evolving systems over time.
│  │  │  ├─ monte_carlo.py  — Sampling and Monte Carlo experiment helpers.
│  │  │  ├─ agent_based.py  — Agent-based simulation scaffolding.
│  │  │  └─ cellular_automata.py  — Grid/state update rules and stepping framework.
│  │  ├─ animation/  — Producing frames and motion data from simulations/models.
│  │  │  ├─ __init__.py  — Animation package marker.
│  │  │  ├─ animation_engine.py  — High-level animation runner (model → frames).
│  │  │  ├─ keyframe_generator.py  — Chooses keyframes and important time points.
│  │  │  ├─ interpolation.py  — Smooths between keyframes (linear, spline, etc.).
│  │  │  └─ export_video.py  — Writes video formats using exporter backends.
│  │  ├─ optimization/  — Parameter fitting, search, and optimization loops.
│  │  │  ├─ __init__.py  — Optimization package marker.
│  │  │  ├─ parameter_sweep.py  — Brute-force grid/random parameter scanning.
│  │  │  ├─ genetic_algorithm.py  — Evolutionary search methods for tough spaces.
│  │  │  └─ gradient_methods.py  — Gradient-based and quasi-Newton methods.
│  │  └─ execution/  — Scheduling and parallel execution for heavy workloads.
│  │     ├─ __init__.py  — Execution package marker.
│  │     ├─ task_queue.py  — Queue abstraction for long-running tasks.
│  │     ├─ scheduler.py  — Schedules tasks (priority, timing, dependencies).
│  │     └─ parallel_executor.py  — Runs tasks in threads/processes safely.
│  ├─ visualization/  — Rendering results: plots, diagrams, animations, exporters.
│  │  ├─ __init__.py  — Visualization module marker.
│  │  ├─ plots/  — Plot primitives, independent of UI framework.
│  │  │  ├─ __init__.py  — Plots package marker.
│  │  │  ├─ plot_base.py  — Common plot interface: data in, figure out.
│  │  │  ├─ line_plot.py  — Line plot rendering logic (time series, curves).
│  │  │  ├─ scatter_plot.py  — Scatter plot logic (points, fitting visuals).
│  │  │  ├─ heatmap.py  — Heatmap rendering logic (2D fields, matrices).
│  │  │  └─ phase_space.py  — Phase space rendering logic for dynamical systems.
│  │  ├─ diagrams/  — Scientific diagrams (not generic charts).
│  │  │  ├─ __init__.py  — Diagrams package marker.
│  │  │  ├─ free_body_diagram.py  — Bodies/forces diagram generator.
│  │  │  ├─ circuit_diagram.py  — Circuit schematic generator (symbolic representation).
│  │  │  ├─ molecular_diagram.py  — Simple molecule diagram generator (2D).
│  │  │  └─ optical_setup.py  — Lenses/mirrors/path diagram generator.
│  │  ├─ animations/  — Animation presets and helpers per category.
│  │  │  ├─ __init__.py  — Animations package marker.
│  │  │  ├─ motion_animation.py  — Motion-focused animation helper.
│  │  │  ├─ wave_animation.py  — Wave propagation/standing wave animation helper.
│  │  │  ├─ field_animation.py  — Field vector/streamline animation helper.
│  │  │  └─ reaction_animation.py  — Reaction progress/energy diagram animation helper.
│  │  ├─ exporters/  — Output formats for images/video.
│  │  │  ├─ __init__.py  — Exporters package marker.
│  │  │  ├─ png_exporter.py  — Writes static PNG outputs.
│  │  │  ├─ svg_exporter.py  — Writes scalable SVG outputs.
│  │  │  ├─ gif_exporter.py  — Writes animated GIF outputs.
│  │  │  └─ mp4_exporter.py  — Writes MP4 video outputs.
│  │  └─ themes/  — Styling presets for consistent visuals.
│  │     ├─ __init__.py  — Themes package marker.
│  │     ├─ dark_theme.py  — Dark UI-friendly defaults.
│  │     ├─ light_theme.py  — Light UI-friendly defaults.
│  │     └─ publication_theme.py  — Print/paper-friendly styling defaults.
│  ├─ orchestration/  — “Tool” composition: pipelines, workflows, scenarios.
│  │  ├─ __init__.py  — Orchestration package marker.
│  │  ├─ tool_descriptor.py  — Declares a tool (inputs, outputs, steps, metadata).
│  │  ├─ tool_builder.py  — Builds executable tools from descriptors/config.
│  │  ├─ workflow.py  — Multi-step workflows (solve → simulate → plot → export).
│  │  ├─ pipeline.py  — Dataflow pipeline abstraction for chaining computations.
│  │  ├─ scenario.py  — Scenario containers (initial conditions + assumptions).
│  │  └─ execution_context.py  — Runtime context passed through tools/workflows.
│  ├─ ai/  — Optional AI features: assistant, retrieval, planning, memory.
│  │  ├─ __init__.py  — AI module marker and toggles.
│  │  ├─ assistant/  — Chat/assistant behaviors and explanation generation.
│  │  │  ├─ __init__.py  — Assistant package marker.
│  │  │  ├─ assistant_core.py  — Main assistant interface: ask → propose → act.
│  │  │  ├─ reasoning.py  — Reasoning scaffolding (step planning, tool calls).
│  │  │  ├─ explanation_generator.py  — Turns results into student-friendly explanations.
│  │  │  └─ prompt_templates.py  — Prompts/instructions used by the assistant.
│  │  ├─ retrieval/  — Knowledge indexing and semantic search for docs/tools.
│  │  │  ├─ __init__.py  — Retrieval package marker.
│  │  │  ├─ knowledge_index.py  — Builds/loads an index of knowledge items.
│  │  │  ├─ embedding_store.py  — Stores vector embeddings and metadata.
│  │  │  └─ semantic_search.py  — Searches index for relevant items.
│  │  ├─ planning/  — Planning which tools/workflows to run for a user goal.
│  │  │  ├─ __init__.py  — Planning package marker.
│  │  │  ├─ task_planner.py  — Breaks a goal into steps and required tools.
│  │  │  └─ tool_selector.py  — Chooses the best tool based on context and constraints.
│  │  └─ memory/  — Session/user memory (optional, can be off by default).
│  │     ├─ __init__.py  — Memory package marker.
│  │     ├─ conversation_memory.py  — Stores conversation context for continuity.
│  │     └─ user_profile.py  — Stores preferences (units, notation, language).
│  ├─ data/  — Static files shipped with the app (tables, constants, metadata).
│  │  ├─ README.md  — Explains what data is stored and how to update it safely.
│  │  ├─ constants/  — Files containing constant tables or sources.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ tables/  — Periodic table, material tables, etc.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ datasets/  — Small example datasets (never huge raw data).
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ reference_models/  — Predefined reference configs/models for demos/tests.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ unit_definitions/  — Canonical unit definitions for consistent conversions.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  └─ metadata/  — Metadata about tools, provenance, versioned sources.
│  │     └─ .keep  — Keeps directory in Git even if empty.
│  ├─ persistence/  — Saving/loading history, favorites, caches, DB schema.
│  │  ├─ __init__.py  — Persistence package marker.
│  │  ├─ database.py  — DB connection/session management and helpers.
│  │  ├─ schema.py  — DB schema definitions (tables/fields/models).
│  │  ├─ migrations/  — Versioned schema changes over time.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ history/  — Structures for storing run history and artifacts.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ favorites.py  — Saved tools/configurations and user favorites.
│  │  └─ cache.py  — Caching layer for computed results and speed-ups.
│  ├─ plugins/  — Plugin system: extend ScienceHub without editing core code.
│  │  ├─ __init__.py  — Plugins package marker.
│  │  ├─ plugin_base.py  — Plugin interface contract (hooks, metadata).
│  │  ├─ plugin_loader.py  — Discovers, validates, loads plugins at runtime.
│  │  ├─ official/  — First-party plugins shipped/maintained with ScienceHub.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ community/  — Third-party plugins (optional).
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  └─ experimental/  — Unstable plugin ideas that may change quickly.
│  │     └─ .keep  — Keeps directory in Git even if empty.
│  ├─ ui/  — UI logic and view models; independent from Qt/Tk/Web.
│  │  ├─ __init__.py  — UI package marker.
│  │  ├─ state.py  — UI state model (selected tool, active tab, plot state).
│  │  ├─ events.py  — UI events (clicked, submitted, exported, etc.).
│  │  ├─ layout/  — Layout descriptors (panel arrangement rules).
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ components/  — Reusable UI components (cards, inputs, charts container).
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ panels/  — Screens/panels (calculator panel, simulation panel, gallery).
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ themes/  — UI theme mapping (fonts, spacing, colors chosen by UI layer).
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  └─ bindings/  — Adapter layer for a concrete UI framework (Qt/Tk/Web/TUI).
│  │     └─styles.css
│  │     └─ .keep  — Keeps directory in Git even if empty.
│  ├─ api/  — External interfaces: CLI, REST, WebSocket schemas.
│  │  ├─ __init__.py  — API package marker.
│  │  ├─ rest/  — REST endpoints for remote use or local web UI.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ websocket/  — Live streaming updates for simulations and plots.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ cli/  — Command-line interface commands (run tool, export, list).
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  └─ schemas/  — Data models for API payloads (requests/responses).
│  │     └─ .keep  — Keeps directory in Git even if empty.
│  ├─ experiments/  — Prototypes and scratch work that can be thrown away safely.
│  │  ├─ __init__.py  — Experiments package marker.
│  │  ├─ sandbox.py  — Quick experimentation entry for trying new ideas.
│  │  ├─ performance_tests.py  — Manual perf checks and profiling scenarios.
│  │  ├─ visual_demos.py  — Demo scripts producing nice visuals for showcasing.
│  │  └─ abandoned_ideas/  — Archived prototypes to reference later.
│  │     └─ .keep  — Keeps directory in Git even if empty.
│  ├─ diagnostics/  — Logging, profiling, telemetry, and health checks.
│  │  ├─ __init__.py  — Diagnostics package marker.
│  │  ├─ logger.py  — Central logging config + helpers.
│  │  ├─ profiler.py  — Profiling utilities and timing helpers.
│  │  ├─ telemetry.py  — Anonymous usage/perf metrics (optional, off by default).
│  │  ├─ error_reports.py  — Error reporting structure and crash logs.
│  │  └─ health_checks.py  — Startup and runtime checks (deps, GPU, permissions).
│  ├─ localization/  — Language packs and translation resources.
│  │  ├─ README.md  — How translation keys work and how to add a language.
│  │  ├─ en/  — English translation resources and defaults.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ de/  — German translation resources.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  ├─ pt_BR/  — Brazilian Portuguese translation resources.
│  │  │  └─ .keep  — Keeps directory in Git even if empty.
│  │  └─ fr/  — French translation resources.
│  │     └─ .keep  — Keeps directory in Git even if empty.
│  ├─ main/  — Has the Main Window logic
│  │  ├─ main_window.py
│  │  ├─ tool_registry.py
│  ├─ config/  — Config files and feature toggles.
│  │  ├─ __init__.py  — Config package marker (and config loader hooks).
│  │  ├─ default.yaml  — Default config shipped with the app.
│  │  ├─ user.yaml  — User overrides (not committed, or committed as template).
│  │  ├─ runtime.yaml  — Runtime-only config (ports, paths, limits).
│  │  └─ feature_flags.yaml  — Feature switches for unstable/experimental features.
│  ├─ utils/  — Small generic utilities used across modules.
│  │  ├─ __init__.py  — Utils package marker.
│  │  ├─ math_helpers.py  — Tiny math helpers not big enough for kernel/domains.
│  │  ├─ formatting.py  — Formatting helpers (sig figs, scientific notation, etc.).
│  │  ├─ time_utils.py  — Timing and date utilities used by logs and runs.
│  │  ├─ file_utils.py  — Safe file read/write helpers and path utilities.
│  │  ├─ decorators.py  — Reusable decorators (timed, cached, validated).
│  │  └─ type_guards.py  — Runtime type guards and validation helpers.
│  └─ tests/  — Automated tests for correctness and stability.
│     ├─ __init__.py  — Tests package marker (optional but convenient).
│     ├─ unit/  — Small, fast tests for single functions/classes.
│     │  └─ .keep  — Keeps directory in Git even if empty.
│     ├─ integration/  — Tests that connect multiple subsystems (engine + kernel + IO).
│     │  └─ .keep  — Keeps directory in Git even if empty.
│     ├─ regression/  — Tests for bugs you fixed so they never return.
│     │  └─ .keep  — Keeps directory in Git even if empty.
│     ├─ performance/  — Performance budgets and benchmarks.
│     │  └─ .keep  — Keeps directory in Git even if empty.
│     └─ fixtures/  — Test data and fixed inputs for repeatable tests.
│        └─ .keep  — Keeps directory in Git even if empty.
